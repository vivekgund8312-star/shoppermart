export default function AboutPage() {
  return (
    <section className="about-page">
      <h1>About this classroom project</h1>
      <p>This shop is a small React example built from the provided <code>data.json</code> dataset.</p>
      <p>It demonstrates components, props, state, <code>useEffect</code>, <code>fetch</code>, <code>filter</code>, and <code>map</code> without extra libraries.</p>
    </section>
  );
}
